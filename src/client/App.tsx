import React from 'react';
import { DashboardWidget } from '../components';

const App: React.FC = () => {
  return (
    <div style={{ maxWidth: '800px', margin: '0 auto' }}>
      <DashboardWidget />
    </div>
  );
};

export default App;